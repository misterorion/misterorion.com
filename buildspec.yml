version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - yarn install --silent
  build:
    commands:
      - yarn run build
artifacts:
  base-directory: public
  files:
    - '**/*'
cache:
  paths:
    - 'node_modules/**/*'